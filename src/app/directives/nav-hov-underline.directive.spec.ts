import { NavHovUnderlineDirective } from './nav-hov-underline.directive';

describe('NavHovUnderlineDirective', () => {
  it('should create an instance', () => {
    const directive = new NavHovUnderlineDirective();
    expect(directive).toBeTruthy();
  });
});
