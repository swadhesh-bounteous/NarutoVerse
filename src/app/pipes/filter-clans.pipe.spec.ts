import { FilterClansPipe } from './filter-clans.pipe';

describe('FilterClansPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClansPipe();
    expect(pipe).toBeTruthy();
  });
});
