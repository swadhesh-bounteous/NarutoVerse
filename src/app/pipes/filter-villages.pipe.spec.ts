import { FilterVillagesPipe } from './filter-villages.pipe';

describe('FilterVillagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVillagesPipe();
    expect(pipe).toBeTruthy();
  });
});
